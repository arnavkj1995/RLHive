# Write a code to plpot something using matplotlib

import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 20, 100)
y = np.sin(x)
plt.plot(x, y)
plt.show()

# Write a code to plot cosine and sine functions using matplotlib

